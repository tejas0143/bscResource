<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.my.csresources">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CSResources">
        <activity android:name="QuestionPaper.QuestionPaper"></activity>
        <activity android:name=".PdfViewer" />
        <activity android:name="Practicals.Practical" />
        <activity android:name=".NoticeActivity" />
        <activity android:name=".ui.main.ContactUs" />
        <activity android:name=".SyllabusAcitvity" />
        <activity android:name=".WebviewActivity" />
        <activity android:name=".BooksActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".DashboardActivity" android:theme="@style/Theme.CSResources"/>
    </application>

</manifest>